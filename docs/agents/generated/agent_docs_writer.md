# CliAgent (`agent_docs_writer.json`)

Rewrites the human-readable agent docs (`docs/agents/<name>.md`) for agent configs that changed, using an LLM CLI: it collects each changed agent's JSON config and JS-action summaries into the input folder, rewrites the human docs, applies them, and regenerates the generated reference docs.

Typically run from CI on pull requests that modify agent configs; the calling workflow commits the resulting diff back to the PR branch.

## Parameters

Configured via `customParams` in the agent JSON or the project `.dmtools/config.js`.

- `agents` — explicit list of agent config names (without `.json`) to document. When set, takes priority over change detection.
- `baseBranch` — git ref to diff against (e.g. `origin/main`); only agents whose JSON changed vs that ref are documented. When neither `agents` nor `baseBranch` is set, all tracked agent configs are documented.

_Human doc: [`agents/docs/agents/agent_docs_writer.md`](agents/docs/agents/agent_docs_writer.md)_

## Attributes

| Attribute | Value |
|---|---|
| ContextId | `agent_docs_writer` |
| outputType | `none` |
| skipAIProcessing | `false` |

## Prompt snapshot

Full prompt / instruction set: [`agents/snapshots/agent_docs_writer.md`](agents/snapshots/agent_docs_writer.md)

## Actions


### preCliJSAction: `prepareAgentDocsUpdate.js`

_Prepare Agent Docs Update — pre-CLI action for agent_docs_writer.json._

- Source: `agents/js/prepareAgentDocsUpdate.js`
- Parameters (customParams):
  - `agents`
  - `agents.length`
  - `baseBranch`


### postJSAction: `applyAgentDocsUpdate.js`

_Apply Agent Docs Update — post-action for agent_docs_writer.json._

- Source: `agents/js/applyAgentDocsUpdate.js`
- Reads:
  - `outputs/agent_docs/`


## LLM step

- outputType: `none`
- Prompt snapshot: `agents/snapshots/agent_docs_writer.md`

## Custom params

- `agents` _(used by JS action)_
- `agents.length` _(used by JS action)_
- `baseBranch` _(used by JS action)_

---
_Generated by js/agentDocGenerator.js_