# story questions

Collects open business questions for a story and creates them as question sub-tickets assigned for review.

## Parameters

Configured via `customParams` in the agent JSON or the project `.dmtools/config.js`.

- `autoStartQuestionAnswer` — auto start question answer.
- `autoStartQuestionAnswerConfigFile` — auto start question answer config file.
- `priorityMap` — priority map.
